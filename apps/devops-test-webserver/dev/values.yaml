replicaCount: 1

image:
  tag: "sha-2eda80b"

envVariables:
  ENV: "development"
  LOG_LEVEL: "debug"

ingress:
  enabled: true
  hosts:
    - host: dev.webserver.internal
      paths:
        - path: /
          pathType: Prefix
